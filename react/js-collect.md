 Js数组常用操作(ES6标准)
``` js
ls = [1,2,3]
// findIndex
ls.findIndex(item => item ===2);
// map
ls.map(item => item +1);
// forEach
ls.forEach(item => console.log(item +5));
// includes
ls.includes(2);
// filter
ls.filter(item => item ===2);
```